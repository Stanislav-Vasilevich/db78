const sections = document.querySelectorAll('section');
const observerPoints = document.querySelectorAll('.observer');
const pointsItem = document.querySelectorAll('.points__item');
// кнопка наверх
const buttonTop = document.querySelector('.top');
// id первой секции каждой страницы
const idFirstSection = sections[0].id;
